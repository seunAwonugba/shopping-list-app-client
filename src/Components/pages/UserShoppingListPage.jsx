
const UserShoppingListPage = () => {

    //We render the current shopping list of the user 
    // for a given data
    // show previous shopping lists
    // Component to create a new list (button)
}

export default UserShoppingListPage;
